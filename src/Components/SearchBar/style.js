import styled from 'styled-components';

const SearchBarWrapper = styled.div`
    width: 60%;
    height: 4vw;
    margin-top: 20vh;
    z-index:1;
    box-shadow: 0 1px 6px rgba(1,1,1,.4);


`;

export {SearchBarWrapper};